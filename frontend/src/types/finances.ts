export type despesa = {
    nome: string
    tipo: string
    descricao: string
    valor: number
    autor: string
    freq: string
}

export type dropProperties = {
    visible: boolean,
    x: number,
    y: number
}